import { FC } from "react"

const Footer: FC = ({}) => {
	return (
		<div className="w-full bg-white p-2 flex items-center justify-between gap-5">
			<span className="text-main">2024</span>
			<span className="text-main">Защищено Кириллом</span>
		</div>
	)
}

export default Footer
